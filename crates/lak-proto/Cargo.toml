[package]
name = "lak-proto"
version = "0.1.0"
edition = "2021"
description = "Linux Agent Kernel — Protocol Buffers definitions"

[build-dependencies]
tonic-build = "0.12"

[dependencies]
tonic.workspace = true
prost.workspace = true
prost-types.workspace = true

[lints]
workspace = true
