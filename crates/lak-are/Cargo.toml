[package]
name = "lak-are"
version = "0.1.0"
edition = "2021"
description = "Linux Agent Kernel — Agent Runtime Environment"

[dependencies]
lak-core = { path = "../lak-core" }
chrono.workspace = true

[lints]
workspace = true
